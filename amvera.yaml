meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: true
  systemPackages:
    - ffmpeg
run:
  scriptName: bot.py
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
